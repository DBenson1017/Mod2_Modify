<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Song Show Page</title>
    <link rel="stylesheet" href="app/assets/stylesheets/users.scss">
</head>

<body>
    <div class="song-show-page-container">
        <div class="song-show-page-wrapper">
            <h1><%=@track.name%></h1>
            <p>By: <%=@track.album.artist.name%></p>
            <p>Presented on: <%=@track.album.name%></p>
        
            <%=form_for @song_playlist do |f|%>
        
            <%= f.label :playlist %>
            <%= f.collection_select :playlist_id, Playlist.all, :id, :name %> 
        
            <%= f.hidden_field :song_id, value: @track.id %> 
        
            <%= f.submit "Add song to playlist"%>
        
            <% end %>
        </div>
        
    </div>
    
</body>
</html>



