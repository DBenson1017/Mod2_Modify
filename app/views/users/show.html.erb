
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="app/assets/stylesheets/users.scss">
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700;900&display=swap" rel="stylesheet">
    <title>Homepage</title>
</head>
<body>
    <div class="index-show-page-wrapper">
        <div class="welcome-wrapper">
            <h1>Welcome to Modify!</h1>
            <h2>Hello <%=@user.name%>!</h2>
            
        <% @user.playlists.each do |p|%>
        <p><%=link_to p.name, playlist_path(p)%></p>
        <%end%>

        <%= form_tag(songs_path , :method => 'get') do %>
        <%= label_tag(:q, "Search for songs:") %>
        <%= text_field_tag(:search, params[:search]) %>
        <%= submit_tag("search") %>
        <% end %>

        </div>
       
     

    </div>    

</body>
</html>
